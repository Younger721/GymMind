spring:
  application:
    name: gymmind-backend
  profiles:
    default: dev
server:
  port: ${SERVER_PORT:8080}
management:
  endpoints:
    web:
      exposure:
        include: health,info
gymmind:
  security:
    jwt:
      issuer: GymMind
      secret: ${JWT_SECRET:}
      access-ttl: PT15M
      refresh-ttl: P7D
