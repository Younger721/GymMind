spring:
  application:
    name: gymmind-backend
  profiles:
    default: dev
server:
  port: ${SERVER_PORT:8080}
management:
  endpoints:
    web:
      exposure:
        include: health,info
platform-admin:
  email: ${PLATFORM_ADMIN_EMAIL:}
  password: ${PLATFORM_ADMIN_PASSWORD:}
gymmind:
  search:
    backend: ${GYMMIND_SEARCH_BACKEND:memory}
    milvus-url: ${GYMMIND_MILVUS_URL:http://localhost:19530}
    elasticsearch-url: ${GYMMIND_ES_URL:http://localhost:9200}
  security:
    jwt:
      issuer: GymMind
      secret: ${JWT_SECRET:}
      access-ttl: PT15M
      refresh-ttl: P7D
