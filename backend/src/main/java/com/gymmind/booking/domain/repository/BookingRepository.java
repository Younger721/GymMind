package com.gymmind.booking.domain.repository; import com.gymmind.booking.domain.model.Booking; import com.gymmind.booking.domain.model.BookingStatus; import java.util.List; import java.util.Optional; public interface BookingRepository{Booking save(Booking b);Optional<Booking> findByTenantIdAndMemberIdAndCourseId(Long t,Long m,Long c);List<Booking> findAllByTenantIdAndStatus(Long tenantId, BookingStatus status);}
