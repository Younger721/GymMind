package com.gymmind.booking.infrastructure.persistence; import com.gymmind.booking.domain.model.Booking; import com.gymmind.booking.domain.model.BookingStatus; import org.springframework.data.jpa.repository.JpaRepository; import java.util.List; import java.util.Optional; interface SpringDataBookingRepository extends JpaRepository<Booking,Long>{Optional<Booking> findByTenantIdAndMemberIdAndCourseId(Long t,Long m,Long c);List<Booking> findAllByTenantIdAndStatus(Long tenantId, BookingStatus status);}
